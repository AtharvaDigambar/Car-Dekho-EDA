-- Create Database
CREATE DATABASE retail_orders;

-- Use Database
USE retail_orders;


-- =========================================
-- 1. Create Categories Table
-- =========================================

CREATE TABLE Categories (
    category_id INT PRIMARY KEY,
    category_name VARCHAR(50) NOT NULL
);


-- =========================================
-- 2. Create Customers Table
-- =========================================

CREATE TABLE Customers (
    customer_id INT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    age INT,
    category_id INT,
    FOREIGN KEY (category_id) REFERENCES Categories(category_id)
);


-- =========================================
-- 3. Create Products Table
-- =========================================

CREATE TABLE Products (
    product_id VARCHAR(10) PRIMARY KEY,
    product_name VARCHAR(100) NOT NULL,
    category_id INT,
    FOREIGN KEY (category_id) REFERENCES Categories(category_id)
);


-- =========================================
-- 4. Create Orders Table
-- =========================================

CREATE TABLE Orders (
    customer_id INT,
    product_id VARCHAR(10),
    quantity INT,
    PRIMARY KEY (customer_id, product_id),
    FOREIGN KEY (customer_id) REFERENCES Customers(customer_id),
    FOREIGN KEY (product_id) REFERENCES Products(product_id)
);


-- =========================================
-- Insert Categories
-- =========================================

INSERT INTO Categories (category_id, category_name)
VALUES
(1, 'Electronics'),
(2, 'Books'),
(3, 'Home'),
(4, 'Sports');


-- =========================================
-- Insert Customers
-- =========================================

INSERT INTO Customers (customer_id, name, age, category_id)
VALUES
(101, 'Aarav Mehta', 24, 1),
(102, 'Bhavna Rao', 29, 2),
(103, 'Chirag Shah', 31, 3),
(104, 'Diya Kumar', 27, 1);


-- =========================================
-- Insert Products
-- =========================================

INSERT INTO Products (product_id, product_name, category_id)
VALUES
('P101', 'Wireless Mouse', 1),
('P102', 'Python Basics', 2),
('P103', 'Table Lamp', 3),
('P104', 'Yoga Mat', 4);


-- =========================================
-- Insert Orders
-- =========================================

INSERT INTO Orders (customer_id, product_id, quantity)
VALUES
(101, 'P101', 2),
(101, 'P102', 1),
(101, 'P103', 3),
(102, 'P102', 2),
(102, 'P103', 1),
(103, 'P103', 2),
(103, 'P104', 1),
(104, 'P101', 1),
(104, 'P102', 2),
(104, 'P103', 1),
(104, 'P104', 3);
